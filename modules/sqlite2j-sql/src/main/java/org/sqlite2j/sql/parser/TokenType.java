package org.sqlite2j.sql.parser;

public enum TokenType {
  CREATE, TABLE, INSERT, INTO, VALUES, SELECT, FROM,
  STAR, LPAREN, RPAREN, COMMA, SEMICOLON,
  EQUAL, BANG_EQUAL, LT, LTE, GT, GTE,
  NULL, AND, OR,
  IDENTIFIER, NUMBER, STRING, EOF
}
